# نسخه ۱۱.۱۸.۰

1. تب مدیرمحور «اسنپ سازمانی» به منوی بالا، سایدبار و بدنه برنامه اضافه شد؛ هیچ نام کاربری/رمزی در پروژه ذخیره نمی‌شود و ورود CAPTCHA عمداً دستی و امن است.
2. بازکردن پنل رسمی، تاریخ پیش‌فرض دیروز، ورود هم‌زمان چند فایل CSV/XLSX، حذف ردیف تکراری دو خروجی، ذخیره گزارش‌ها و نمایش ستون‌های ۱،۲،۵،۹،۱۲،۱۵،۱۸،۱۹،۲۴ اضافه شد.
3. چهار فیلد سال، ماه، از تاریخ و تا تاریخ + جمع کل، تعداد سفر و تجمیع به تفکیک نماینده اضافه شد.
4. خروجی اکسل نمای فیلترشده اضافه و باگ دانلود دوتایی XLS+CSV حذف شد؛ هر کلیک فقط یک فایل می‌گیرد.
5. رنگ سراسری کلیدهای اکسل سبز و حذف قرمز شد.
6. موقعیت زنده دارای گزینه «همه نمایندگان»، fitBounds هم‌زمان و ستون آدرس متنی خودکار برای هر نماینده شد.
7. قانون امنیتی ۶۹: اسرار چت هرگز در کد/chat.arena/ZIP ثبت نشوند و CAPTCHA دور زده نشود.

# نسخه ۱۱.۱۷.۱

1. باگ اصلی «تعداد خالی = ۱» در مسیر فعال ذخیره سفارش حذف شد؛ کالای با تعداد صفر/خالی وارد سفارش، ارسال یا جمع مبلغ نمی‌شود. رکوردهای قدیمی با الگوی مشخص این خطا نیز هنگام ارسال پاک‌سازی می‌شوند.
2. تعیین اطلاعات پیام‌رسان از حالت فهرست ثابت خارج شد و تمام ستون‌های واقعی و سفارشی لیست‌های داروخانه، پزشک و سفارش را پویا می‌خواند؛ تیک برداشته‌شده قطعاً از متن حذف می‌شود.
3. مدیر کشویی‌ها تمام عرض پنل را می‌گیرد و کارت‌ها کنار هم‌اند؛ پنل قدیمی مخفی شد و هر تب فقط شناسه‌های متعلق به خودش را نشان می‌دهد.
4. نام‌های داروخانه/پزشک دیگر یکجا رندر نمی‌شوند؛ جستجوی حداقل دو حرف، حداکثر ۵۰ نتیجه با ویرایش/حذف نشان می‌دهد و برای بیش از ۱۰هزار رکورد آماده است.
5. تاریخ سفارش همیشه فعال است؛ نام داروخانه پس از ثبت یا خروج/بازگشت به تب پاک می‌شود؛ هشدار جایگذاری به‌صورت شناور کنار ورودی است و ترتیب فرم را جابه‌جا نمی‌کند.
6. نشان نسخه فقط برای مدیر/سرپرست نمایش داده می‌شود.
7. ذخیره و اعمال دوباره ترتیب، ترتیب لیست، عرض و ارتفاع پیکسلی اطلاعات کالا در لایه نهایی تضمین شد.

# نسخه ۱۱.۱۷.۰

1. شناسه و جای فیلدها تثبیت شد؛ جابه‌جایی خودکار DOM هنگام ورود نسخه جدید متوقف شد.
2. هشدار داروخانه تکراری سفارش کنار همان فیلد نمایش داده می‌شود و جایگذاری، استان/شهر/منطقه/آدرس و فیلدهای سفارشی متناظر را دقیق پر می‌کند.
3. مدیر کشویی‌ها در ابتدای تب افزودن‌ها، با کارت‌های چندستونه واقعی و عنوان‌های کاملاً فارسی نمایش داده می‌شود؛ سال/ماه از مدیر کشویی حذف‌اند.
4. مدیریت نام داروخانه و پزشک همراه زیرمجموعه‌ها، ویرایش ارجاع‌ها و حذف تأییدشده اطلاعات وابسته اضافه شد.
5. همه لیست‌های اصلی تازه‌ترین رکورد را بالا نشان می‌دهند. مدیر تعیین می‌کند چه فیلدهایی در پیام‌رسان‌ها ارسال شود و کاربر حق تغییر ندارد.
6. GPS با شروع ویزیت فعال می‌شود و کارت‌های مسافت متر، توقف، نقاط و ساعت شروع را نشان می‌دهد. پایان ویزیت، سابقه دائمی تفکیک‌شده را در رصد تردد ثبت می‌کند.
7. رصد تردد دارای جستجوی لحظه‌ای و خروجی اکسل است؛ قانون ردیف/نام نماینده برای همه خروجی‌ها تثبیت شد.
8. آدرس معکوس با جزئیات کوچه/راه/پلاک و نمایش کامل Nominatim ذخیره می‌شود.
9. نسخه دقیق ۱۱.۱۷.۰ در هدر دیده می‌شود. workflow گیت‌هاب از دستور اشتباه Next.js پاک شد تا ضربدر قرمز کاذب برطرف شود.

# نسخه ۱۱.۱۶.۲

1. اصلاحات مدیر کشویی‌های افزودن‌ها (به گزارش کاربر): کارت‌های فیلدها حالا کنار هم (شبکه‌ای) چیده می‌شوند نه همه زیر هم؛ نام‌های انگلیسی با واژه‌نامه فارسی + خواندن placeholder رفع شد؛ فیلدهای «سال» و «ماه» تاریخ شمسی از این بخش حذف شدند و «نوع داروخانه» مثل بقیه کشویی‌ها یک کارت عادی است.
2. رفع ناهماهنگی طوسی‌ها: موتور طوسی حالا روی کل بلوک فیلد (شامل ورودی دیداری کمبوها) اعمال می‌شود — دیگر «نصف طوسی نصف سفید» نیست؛ و با حفظ موقعیت اسکرول، پدیده «کشیده شدن فیلد به بالا بعد از انتخاب استان» برطرف شد.
3. سفارشات: فیلدهای کشویی طوسی واقعاً غیرقابل‌انتخاب می‌شوند (قفل روی خود کمبو)، و برخلاف قبل، بخش کالا (تعداد کالا/جایزه/کاتالوگ) همیشه فعال و عادی می‌ماند. اگر جای فیلدها در تب داروخانه عوض شود، همان ترتیب در سفارشات هم اعمال می‌شود.
4. دکمه «تغییر رمز» دیگر روی دکمه خروج نیفتاده؛ داخل هدر درست کنار خروج می‌نشیند.
5. رفع هشدار اشتباه «قبلاً ثبت شده» برای رکورد جدید: امضای رکوردی که لحظه‌ای پیش خودکار ذخیره شده به خاطر سپرده می‌شود و ثبت دوباره‌اش با پیام دوستانه (بدون ایجاد رکورد تکراری) جلوگیری می‌شود؛ رکوردِ در حال ویرایش هم از بررسی تکراری مستثنا است.
6. قانون تکیدی کاربر: ابتدا PROJECT_GRAPH.md خوانده شود، نه کل کدها (همان قانون ۶۶ — تأیید مجدد).

# نسخه ۱۱.۱۶.۱

1. رفع مشکل آپدیت نشدن گیت‌هاب: مشخص شد روی گیت‌هاب یک کامیت دستی «پروژه اولیه» با محتوای 11.15.3 جایگزین تاریخچه شده و به‌خاطر نخوردن تاریخچه‌ها push رد می‌شد. اسکریپت PUSH_FRESH_GITHUB.bat ساخته شد: ثبت فایل‌ها ← ادغام امن با allow-unrelated-histories (فایل‌های شما برنده) ← push. بعد از این یک‌بار، SYNC_ALL.bat دوباره مثل قبل کار می‌کند.
2. گیت‌لب بدون توکن: روش کلید SSH به RAHNAMA_GITLAB.txt اضافه شد (ssh-keygen ← افزودن کلید در SSH Keys ← تغییر ریموت به git@gitlab.com). تا وقتی گیت‌لب وصل نشود، SYNC_ALL.bat خودش تشخیص می‌دهد و فقط گیت‌هاب را به‌روز می‌کند؛ پشتیبانی گیت‌لب در فایل‌ها حفظ است.
3. تغییری در صفحات برنامه نسبت به ۱۱.۱۶.۰ نیست (نسخه ترمیمی تحویل/ابزار).

# نسخه ۱۱.۱۶.۰

1. گراف دانش پروژه: فایل PROJECT_GRAPH.md ساخته شد که همه فایل‌ها، توابع، روابط window بین لایه‌ها، نقشه API، نقشه حافظه و نقشه تب‌ها را یکجا دارد تا هوش مصنوعی به‌جای خواندن کل سورس فقط همین را بخواند (صرفه‌جویی توکن). سازنده خودکار: update_project_graph.py — در پایان هر تحویل بازسازی می‌شود.
2. تب افزودن‌ها: هر فیلد کشویی هر تب با نام فارسی نمایش داده می‌شود و همه زیرمجموعه‌هایش دقیقاً زیر همان فیلد (زیر هم) آمده‌اند؛ کنار هر زیرمجموعه دکمه ✏️ ویرایش و 🗑️ حذف است، هر فیلد جستجوی لحظه‌ای و کادر افزودن گزینه دارد و با افزودن/حذف فیلد کشویی جدید، همین بخش خودکار به‌روز می‌شود.
3. زنجیره طوسی: فیلدهای وابسته (مثل شهر/منطقه) تا انتخاب والد (مثل استان) طوسی و غیرفعال‌اند و بعد از انتخاب والد عادی می‌شوند. مدیر برای هر فیلد می‌تواند والدش را مشخص یا حالت را خاموش کند و یک کلید کلی هم در افزودن‌ها هست.
4. تب سفارشات: تا وقتی داروخانه انتخاب و «جایگذاری خودکار» نشده، همه فیلدها جز نام داروخانه طوسی‌اند؛ بعد از جایگذاری، اطلاعات داروخانه سر جایشان می‌نشیند (طوسی می‌مانند) و فقط بخش کالا (تعداد کالا/جایزه) فعال و عادی می‌شود. داروخانه‌های ثبت‌شده همه کاربران یکجا در سفارشات دیده می‌شود.
5. تکراری‌گیر دو مرحله‌ای: اگر اطلاعات داروخانه/پزشک دقیقاً مثل رکورد موجود باشد، ثبت کاملاً مسدود می‌شود و پیام می‌دهد «فقط ویرایش از لیست مجاز است» (چه خودت ثبت کرده باشی چه کاربر دیگر). اگر فقط نام یا تلفن نزدیک بود، همان پنجره قبلی تأیید/انصراف می‌آید.
6. فلش‌های بالا/پایین ورودی‌های عددی تعداد کالا و تعداد جایزه در تب سفارشات برداشته شد.
7. دکمه «🔑 تغییر رمز» بالای همه صفحه‌ها: هر کاربر رمز خودش را عوض می‌کند و همان لحظه در سیستم ورود (CRM_USERS_AUTH + state.users) اعمال می‌شود.
8. سطوح دسترسی آماده در تب کاربران و دسترسی: نماینده علمی، کارشناس فروش، سرپرست — با یک انتخاب، همه تیک‌های دسترسی کاربر یکجا تنظیم می‌شود؛ برای کاربر جدید یا موجود، بدون تعریف دوباره.
9. عیب‌یابی سالم شد: خطای «شی state در دسترس نیست» رفع شد (state حالا در window در دسترس است)، ردیف /api/state دیگر هشدار بی‌ربط نمی‌دهد («ذخیره سروری غیرفعال — عادی») و متن قدیمی بالای صفحه جایگزین شد.
10. در تب داروخانه اگر نام داروخانه جدید را با «➕ افزودن» لحظه‌ای اضافه کنی، رکورد خودش خودکار ذخیره می‌شود؛ نیازی به زدن جداگانه دکمه ذخیره نیست.
11. رفع نقص ورود فیلد جدید به بخش کالا (ستون‌ها و کالاها): فیلدها حالا با اندازه و شماره ترتیب واقعی وارد فرم کالا می‌شوند (در انتظار تأیید مرورگری). اعمال «شماره ترتیب لیست» روی جدول‌های لیست تقویت شد: ردیف همیشه ستون اول و نام نماینده ستون دوم است.
12. هر فیلدی که مدیر در تب داروخانه اضافه/حذف کند، خودکار در تب سفارشات هم اضافه/حذف می‌شود (همگام‌سازی آینه‌ای).

# نسخه ۱۱.۱۵.۳

1. قانون جدید کاربر برای همگام‌سازی فایل‌ها: اسکریپت «SYNC_ALL.bat» (ویندوز) و «sync_all.sh» (لینوکس/مک) ساخته شد؛ با یک دستور، تغییرات محلی اول از گیت‌هاب و گیت‌لب pull و بعد به هر دو push می‌شود. راهنمای یک‌بار تنظیم گیت‌لب در RAHNAMA_GITLAB.txt آمده است.
2. فایل‌ها مستقل از سیستم عامل شدند: «.gitattributes» اضافه شد تا خط‌پایان‌ها بین ویندوز و لینوکس/مک به‌هم نریزد (bat/ps1 با CRLF و sh/py/mjs با LF)؛ چون کاربر با دو سیستم کار می‌کند و فولدر برنامه را مدام بین دو سیستم جابه‌جا می‌کند.
3. امنیت .env: بررسی کل تاریخچه گیت نشان داد هیچ فایل .env واقعی تاکنون وارد گیت‌هاب نشده است. .gitignore محکم‌تر شد (هر .env و .env.* به‌جز قالب‌های example؛ node_modules در هر سطح؛ server-db.json؛ *.zip) و رمز نمونه admin1234 از فایل قالب .env.ndcohub.example حذف شد.
4. node_modules برای همیشه غیرضروری شد: بررسی نشان داد server.js و همه اسکریپت‌های scripts فقط از ماژول‌های داخلی Node استفاده می‌کنند؛ وابستگی‌های بلااستفاده express و cors از package.json حذف شد و package-lock.json سبک بازسازی شد. دیگر npm install لازم نیست؛ حذف امن روی ویندوز: rmdir /s /q node_modules و روی لینوکس/مک: rm -rf node_modules.
5. مرحله build فایل .gitlab-ci.yml از مراحل خفته اسکلت Next (typegen/tsc) پاک شد تا CI گیت‌لب سبز بماند.
6. لیست سفید پاکسازی: KEEP_ONLY_GITHUB.bat که جا افتاده بود اضافه شد و فایل‌های جدید این نسخه هم به OFFICIAL_FILELIST.txt افزوده شدند (۲۳۹ ورودی). در این نسخه هیچ تغییری در صفحات و فرم‌های برنامه نداریم.

# نسخه ۱۱.۱۵.۲

1. هشدار «داروخانه/پزشک تکراری» دیگر فقط یک دکمه OK ندارد؛ به پنجره انتخاب دودکمه‌ای تبدیل شد: تأیید/بله = با این حال ذخیره شود، انصراف/خیر = ذخیره نشود. رفتار قبلی (ذخیره خودکار بعد از OK) حذف شد.
2. این تبدیل در هر ۴ مسیر ثبت اعمال شد: داروخانه و پزشک در crm-features-v9.js (مسیر فعال) و دو نسخه تکراری در crm-app.js.

# نسخه ۱۱.۱۵.۱

1. ریشه اصلی اخطار اجباری پیدا و حذف شد: ۹ بررسی سخت‌کدشده (بدون ربط به سیستم ستاره) در ذخیره داروخانه، پزشک و سفارش — سه‌تایی در crm-features-v9.js و شش‌تایی در دو نسل کد crm-app.js — حذف شدند. پیام‌هایی مثل «لطفاً نام، استان، شهر، منطقه و آدرس/لوکیشن داروخانه را کامل کنید.» دیگر بدون توجه به ستاره اجرا نمی‌شوند.
2. از این نسخه به بعد در کل برنامه و همه تب‌ها فقط فیلدهایی که مدیر ستاره‌دار کرده موقع ذخیره اجبار می‌کنند؛ همه مسیرهای ذخیره به اعتبارسنج واحد ستاره‌محور (validateRequiredFields) وصل شدند که فیلدهای مخفی یا حذف‌شده را هم نادیده می‌گیرد.
3. بررسی «حداقل یک قلم کالا در سفارش» به‌عنوان قانون کسب‌وکار حفظ شد (ربطی به ستاره فیلدها ندارد).

# نسخه ۱۱.۱۵.۰

1. اجبار ستاره‌دار قدیمی پاک شد؛ فقط «نام داروخانه» و «نام پزشک» به‌صورت پیش‌فرض الزامی‌اند و ثبت دیگر به‌خاطر فیلدهای خالی دیگر اخطار نمی‌دهد (مهاجرت یک‌باره روی داده فعلی اعمال می‌شود).
2. در تب افزودن‌ها کنترل‌های هر تب کنار هم چیده شد؛ جعبه «فهرست فشرده همه کشویی‌ها» و جعبه «اطلاعات فیلدها» از این تب برداشته شد؛ دکمه رنگی کوچک «افزودن گزینه جدید» حذف شد و حالا با تایپ داخل کشویی، گزینه «➕ افزودن "متن"» همان لحظه به زیرمجموعه همان کشویی اضافه می‌شود. وابستگی استان/شهر/منطقه حفظ شده است.
3. همه دکمه‌های حذف برنامه آیکن 🗑️ و همه دکمه‌های ویرایش آیکن ✏️ شدند.
4. تیک «افزودن لحظه‌ای گزینه» برای همه انواع فیلد در افزودن فیلد و ویرایش فیلد فعال است و برای فیلدهای ثابت فرم هم ذخیره می‌شود.
5. ستون اول لیست‌ها ردیف و ستون دوم نام نماینده است؛ «شماره ترتیب در لیست» حالا واقعاً ترتیب ستون‌های جدول لیست را عوض می‌کند (جدا از ترتیب فرم).
6. ستون مسیریابی در لیست‌ها به‌جای متن، آیکن نقشه/GPS در هر سطر دارد.
7. دکمه «پاک کردن» به فرم‌های داروخانه‌ها، پزشکان و سفارشات اضافه شد.
8. حذف فیلد در جدول اطلاعات فیلدها فیلد را کاملاً حذف می‌کند (نه فقط مخفی)؛ برداشتن تیک «نمایش در فرم» دیگر دکمه حذف را به «نمایش مجدد» تبدیل نمی‌کند.
9. کنار دکمه حذف هر قلم سفارش، دکمه ویرایش ✏️ اضافه شد که کالای همان سطر را در تب ستون‌ها و کالاها برای ویرایش باز می‌کند.
10. «اطلاعات کادرها» و «اطلاعات کلیدها» فقط در بخش خودشان نمایش داده می‌شوند و از جدول اطلاعات فیلدها برداشته شدند.
11. کالاها: دکمه «+کالای جدید» حذف شد؛ ورودی‌های «افزودن فیلد به فرم کالا» لیبل گرفتند؛ «شماره ترتیب در کادر کالا» و «شماره ترتیب در لیست» جدا هستند و واقعاً کار می‌کنند؛ فیلدهای تازه درست زیر همان کادر در «اطلاعات کالا» می‌آیند (ویرایش عرض/ارتفاع/ترتیب + جدول کالاها با ویرایش/حذف کارا)؛ دکمه «ویرایش جدول» حذف شد؛ «تعداد» به «تعداد کالا» تغییر نام داد؛ تصویر جایزه برداشته شد؛ هر سطر اقلام «جمع مبلغ» فقط‌خواندنی دارد (تعداد کالا × قیمت واحد داروخانه)؛ درصد ارزش افزوده نمایش داده می‌شود و فقط مدیر می‌تواند تغییرش دهد؛ دو جمع کل فقط‌خواندنی (بدون ارزش افزوده / با ارزش افزوده) زیر اقلام است؛ فرمول جمع (کدام فیلد × ÷ + − کدام فیلد) در اطلاعات کالا برای مدیر قابل تنظیم است؛ فلش کشویی نوع فیلد دیگر لبه صفحه نمی‌افتد؛ صفر پیش‌فرض با اولین تایپ پاک می‌شود؛ همه اعداد برنامه جداکننده سه‌رقمی دارند؛ جمع کل سفارش اصلاح شد؛ با افزودن کالا به سفارش، تعداد کالا و تعداد جایزه برای تنظیم مدیر نمایش داده می‌شود.
12. صفحه پشتیبان‌گیری از چیدمان خودکار مستثنا شد تا به‌هم نریزد؛ پشتیبان خودکار واقعاً اجرا می‌شود؛ پشتیبان دستی در پوشه‌ای که مدیر انتخاب می‌کند ذخیره می‌شود (در Chrome/Edge با انتخاب پوشه و دسترسی ذخیره‌شده برای دفعات بعد؛ در سایر مرورگرها با پنجره ذخیره در محل دلخواه)، نه بی‌صدا در Downloads.
13. عیب‌یابی ریز شد: هر بخش جدا گزارش می‌شود — وضعیت آنلاین، حجم localStorage، نشست کاربر، شمارش داده‌ها، فیلدهای سفارشی/حذف‌شده، آخرین پشتیبان، سرویس‌ورکر، کش‌ها، IndexedDB، File System Access، Leaflet، سلامت سرور با زمان پاسخ و نسخه — و جمع‌بندی مشکل‌ها در کادر وضعیت می‌آید؛ دکمه «بررسی مجدد اتصال» حالا همین عیب‌یابی واقعی را اجرا می‌کند نه تست ساختگی.
14. قانون دائمی ثبت شد: هر امکانِ اضافه/حذف/تغییرشده باید در «دسترسی‌ها» هم منعکس شود. گروه «ابزارهای مدیریت (نسخه ۱۱.۱۵)» با کلیدهای جدید (طراحی دستی، کپی تب‌ها، افزودن‌ها، ویرایش جغرافیا، پشتیبان، بازیابی، عیب‌یابی، نصب، تارگت فروش، فرمول سفارش) به صفحه دسترسی‌ها اضافه شد.

# نسخه ۱۱.۱۴.۰

1. ستاره سیاه لیبل حذف شد؛ ستاره قرمز بدون فاصله دقیقاً کنار متن لیبل می‌نشیند. ثبت با فیلد خالی ستاره‌دار اخطار فارسی می‌دهد: «فیلد «X» خالی است و باید پر شود.» اخطار انگلیسی مرورگر با novalidate قطع شد.
2. در اضافه کردن کالا: شماره ترتیب، زیرهم/روبرو، عرض و ارتفاع پیکسل به نوار افزودن فیلد آمد.
3. ستون «سایز لیبل/فیلد» شد «سایز فونت لیبل/فیلد» با محدوده ۱۰–۲۸ تا عدد بزرگ فونت را غول نکند و فیلدهای بعدی از صفحه نپرند.
4. ترتیب فرم و ترتیب لیست جدا شد؛ هر دو ستون در اطلاعات فیلدها هست و همه فیلدهای همان تب دیده می‌شوند.
5. کشویی «چه چیزی اضافه شود؟» فیلد / کادر / کلید. جدول اطلاعات کادرها و اطلاعات کلیدها با ویرایش/حذف مثل اطلاعات فیلدها اضافه شد.
6. کنار عرض، ارتفاع هست. عرض و ارتفاع عدد واقعی همان فیلد/کادر/کلید را نشان می‌دهد نه همیشه ۲۲۰.
7. کلید حذف ساخته‌شده کنار سطر همان ردیف را حذف می‌کند. ثبت/ویرایش/حذف پیغام تأیید دارند. حذف قرمز، ذخیره سبز تیل، ویرایش outline.
8. روی بوم خط‌دار طراحی دستی کادر، کلید، فیلد و کارت قابل انتخاب/درگ است (تاریخ تقویم کلیک را قفل نمی‌کند). کپی تب: کل امکانات یا انتخاب بخشی با تیک ریز.

# نسخه ۱۱.۱۳.۰

1. گزینه «زیر هم» دیگر با عرض فیلد خنثی نمی‌شود؛ فیلد یک سطر جدا می‌گیرد و عرض فقط خود اینپوت را بزرگ می‌کند.
2. عرض فیلد در فرم افزودن/ویرایش است؛ ستون عرض از جدول اطلاعات فیلدها برداشته شد.
3. بعد از ذخیره ویرایش، کادر اطلاعات فیلدها همان‌جا (با همان اسکرول) برمی‌گردد.
4. حذف یک فیلد دیگر فیلدهای بعدی همان کادر را پنهان نمی‌کند.
5. شناسه‌های لاتین فیلد/کلید با نام فارسی نشان داده می‌شوند.
6. تیک ستاره‌دار ستاره قرمز کنار لیبل می‌گذارد و پر کردن را الزامی می‌کند.
7. داروخانه ثبت‌شده در تب داروخانه‌ها در سفارشات مثل پیش‌فرض‌ها اخطار «قبلاً ثبت شده» و پر کردن خودکار می‌دهد.
8. فیلدهای روبرو روی یک خط تراز شدند (تاریخ ثبت دیگر پایین‌تر نیست).
9. طراحی دستی: بوم خط‌دار، کادر فرم ثبت هدف درگ نیست، فیلدها قفل و غیرقابل تایپ‌اند، اندازه از گوشه عوض می‌شود.
10. تیک «خروجی اکسل» در افزودن فیلد و جدول اطلاعات؛ ستون تیک‌خورده در خروجی اکسل می‌آید.

# نسخه ۱۱.۱۲.۰

1. کشویی‌ها (استان و بقیه) با تایپ لحظه‌ای فیلتر می‌شوند؛ حرف اول همان‌هایی را می‌آورد که با آن شروع می‌شوند.
2. تب افزودن فشرده‌تر شد. برای شهر اول استان انتخاب می‌شود بعد شهرهای همان استان؛ منطقه هم بعد از شهر.
3. نام داروخانه‌ای که در تب داروخانه اضافه شود در کل برنامه (سفارشات، جستجو، تکراری) همان رکورد است.
4. فریز سرستون و نوار اسکرول افقی زیر سرستون بعد از ساخته‌شدن جدول طراح دوباره اعمال می‌شود. سایز عددی عرض خود فیلد را عوض می‌کند.
5. در طراحی دستی روی فیلد/کلید یک لایه قفل است؛ کلیک انتخاب می‌کند نه پر کردن فرم.

# نسخه ۱۱.۱۱.۰

1. در ستون‌ها اندازه فیلد دیگر برای همه ۲۲۰ نیست؛ آدرس بزرگ‌تر دیده می‌شود و تغییر اندازه روی خود فیلد ذخیره و اعمال می‌شود.
2. سرستون جدول فریز است و نوار اسکرول افقی همان کادر، زیر سرستون می‌ماند نه ته جدول.
3. دکمه افزودن فیلد ساده دوباره به `customFields` می‌نویسد (کلید type=button + ذخیره مستقیم).
4. تنظیمات قبلی کاربر پاک نمی‌شود؛ قبل از این نسخه یک پشتیبان محلی گرفته می‌شود. فیلد داروخانه سفارش دیگر تمام‌عرض اجباری نیست.
5. طراحی دستی: فیلدها حالت پر کردن فرم ندارند؛ کلیک = انتخاب برای اسم و اندازه. تغییر اندازه فرم ثبت، لیست را حذف نمی‌کند.

# نسخه ۱۱.۱۰.۰

1. تب افزودن‌ها مثل ستون‌ها گرید همه تب‌ها را دارد؛ کشویی همان تب با زیرمجموعه و ویرایش/حذف روبه‌رو.
2. در اضافه کردن کالا می‌شود فیلد ساخت و کالا را ویرایش کرد.
3. ذخیره فیلد ساده: مقدار فیلد سفارشی دیگر هنگام ثبت داروخانه/پزشک/سفارش/کالا پاک نمی‌شود.
4. در سفارش، با تایپ نام داروخانه همه داروخانه‌های هم‌نام با استان/شهر/آدرس می‌آید تا یکی انتخاب شود.
5. کنار حذف تب، ویرایش نام و شماره تب اضافه شد.
6. کپی‌از / کپی‌به در طراحی دستی دیگر زیر بوم خط‌کشی نمی‌رود.

# نسخه ۱۱.۹.۱

1. روز هفته تقویم شمسی با تاریخ واقعی تهران جور است (۱ مرداد ۱۴۰۵ = پنجشنبه؛ امروز ۲۲ مرداد = پنجشنبه).
2. کادر کوچک میلادی فقط JAN…DEC و روز تهران را نشان می‌دهد (مثلاً AUG ۱۳)، نه نام ماه محلی.
3. فیلد تاریخ ساخته‌شده توسط مدیر همان تقویم کنار فیلد تاریخ داروخانه را دارد.
4. در فیلدهای ساده، با تایپ اسم دکمه «افزودن «متن»» کنار همان فیلد ظاهر می‌شود.

# نسخه ۱۱.۹.۰

1. امکانات آماده روی تب جدید واقعاً یک فیلد/کلید می‌سازد (نه سکوت، نه ده تا).
2. تب تازه‌ساخته خالی است؛ مربع‌های ثبت/بازنشانی و کادر لیست پیش‌فرض حذف شد.
3. طراحی دستی: همان امکانات آماده + ساخت کادر؛ همه فیلد و کلید و نقشه قابل درگ است.
4. برچسب رنگی می‌گوید کلید برای ردیف است، کادر است یا کل فرم.

# نسخه ۱۱.۸.۰

1. امکانات آماده روی تب جدید دیگر با هر کلیک ۱۰ بار ساخته نمی‌شود؛ هر کلید یک‌بار می‌آید.
2. تب «طراحی دستی تب‌ها»: همه تب‌ها مثل ستون‌ها اینجاست؛ با کلیک همان صفحه می‌آید.
3. درگ چپ/راست/بالا/پایین و تغییر اندازه فیلد، کلید، کادر و نقشه.
4. کپی امکانات و چیدمان از یک تب به تب دیگر.
5. شکل نقشه ایران عوض نشده است.

# پاکسازی درخت فایل (همراه ۱۱.۷.۰)

روی گیت‌هاب این فایل‌های اضافه بود: زباله WinRAR، crm-enhancements.js،
iran-provinces.geojson، icon/logo.svg و src-svg.
از این محیط پوش به گیت‌هاب ممکن نیست. روی ویندوز:
CLEAN_EXTRA_FILES.bat و بعد PUSH_GITHUB_CLEAN.bat را اجرا کن.

# نسخه ۱۱.۷.۰

1. تب جدید مدیر به منوی اصلی و سایدبار هم اضافه می‌شود (قبلاً فقط داخل ستون‌ها دیده می‌شد).
2. مدیر شماره ترتیب همه تب‌ها (قدیمی و جدید) را مشخص می‌کند.
3. ساخت کادر: نام، تب مقصد، و انتخاب فیلد/کلید برای گذاشتن داخل کادر.
4. امکانات آماده شامل فیلد متنی/عددی/تاریخ/کشویی/تلفن/متن بلند + نقشه و کلیدها.
5. جای فیلد (روبرو/زیر هم) و داخل کدام کادر هنگام ثبت فیلد مشخص می‌شود.
6. کلید ویرایش و حذف لیست‌ها مخفی نمی‌شود.
7. لوگوی TENIN TEB TAHA طبق تصویر کاربر بزرگ و واضح در هدر، سایدبار و ورود.

# نسخه ۱۱.۶.۰

1. فیلد اضافه‌شده دیگر پاک و دوباره ساخته نمی‌شود؛ روی گرید اصلی همان تب می‌نشیند.
2. برای هر کادر دکمه ویرایش هست (ثبت کادر از قبل بود).
3. کلیدها و امکانات اصلی (موقعیت فعلی، دریافت آدرس، نقشه، ثبت، حذف، ویرایش، فایل…) در طراح ستون‌ها می‌آیند تا مدیر داخل کادر بگذارد.
4. مدیر می‌تواند تب جدید بسازد؛ تب به منو اضافه می‌شود، آیکون از روی اسم می‌آید، فونت و اندازه مثل تب‌های فعلی است و با همان امکانات طراحی می‌شود.

# نسخه ۱۱.۵.۱

1. تب‌های اصلی برگشت: خطای نحوی انتهای crm-app.js منو را می‌خواباند و فقط داشبورد دیده می‌شد.
2. منوی بالا و سایدبار دیگر در HTML هم هستند؛ اگر جاوااسکریپت خطا بدهد تب‌ها حذف نمی‌شوند.
3. تب‌های داخل «ستون‌ها و کالاها» که با CSS مخفی شده بودند دوباره دیده می‌شوند و هم‌اندازه تب داشبورد هستند.

# نسخه ۱۱.۵.۰

1. فونت لیبل و فونت خود فیلد جدا + سایز عددی.
2. تب‌های ستون‌ها هم‌اندازه تب داشبورد.
3. سرستون جداول هنگام اسکرول فریز است.
4. ستاره الزامی برای هر فیلد + اخطار اگر خالی بماند.
5. دکمه‌های هم‌نام یک شکل شدند.
6. کادر اقلام سفارش دیگر تکرار نمی‌شود؛ می‌توان کادر گروهی ساخت و فیلدها را داخلش گذاشت.

# نسخه ۱۱.۴.۱

1. تکرار فیلد اقلام سفارش در طراح ستون‌ها حذف شد (فقط ۴ ستون کالا/تعداد/جایزه/قیمت یک‌بار می‌آید).
2. دکمه ویرایش کنار هر کالا در اضافه کردن کالا.
3. چینش روبرو/زیر هم واقعاً روی فرم اعمال می‌شود.
4. برای هر فیلد: فونت، بولد، اندازه نوشته.

# نسخه ۱۱.۴.۰

1. فیلد جدید در تب ستون‌ها واقعاً روی فرم تب اصلی همان‌جا می‌نشیند.
2. شماره ردیف = جای واقعی فیلد در فرم (۵ یعنی پنجمین فیلد).
3. چینش «روبرو» یا «زیر هم» برای هر فیلد.
4. تیک نمایش در فرم روی خود فرم اعمال می‌شود (نه فقط لیست).
5. همه فیلدهای صفحه (از جمله عرض/طول و سطر اقلام سفارش) در لیست طراح می‌آیند.
6. همه کشویی‌های سیستم در تب افزودن‌ها لیست می‌شوند و می‌شود گزینه اضافه کرد.

# نسخه ۱۱.۳.۰

1. ستون ترتیب مثل شماره ردیف است: عدد ۴ یعنی همان فیلد می‌رود ردیف ۴ فرم و جدول.
2. دکمه‌های ▲▼ فیلد را یکی‌یکی بالا/پایین می‌برند و شماره‌ها از نو ۱،۲،۳ می‌شوند.
3. اندازه (پیکسل) عرض واقعی فیلد در فرم را عوض می‌کند.
4. تیک «نمایش در فرم» و «نمایش در لیست» برای همه فیلدهای موجود (ثابت و اضافه‌شده) اضافه شد.

# نسخه ۱۱.۲.۰

1. در ستون‌ها و کالاها، همه فیلدهای فرم همان تب دیده می‌شود (ثابت بالای کادر درصد + سفارشی).
2. روبه‌روی هر فیلد دکمه ویرایش هست.
3. حذف فیلد، همان فیلد را از فرم تب اصلی هم برمی‌دارد (نه فقط از لیست طراح).
4. هر کالای ثبت‌شده در تب سفارشات با فیلد تعداد جایزه ظاهر می‌شود.

# نسخه ۱۱.۱.۰

1. تب «ستون‌ها و کالاها»: همه تب‌های برنامه مثل داشبورد به‌صورت کارت داخل همین صفحه می‌آید.
2. با کلیک روی هر تب، فقط حذف و اضافهٔ فیلد همان تب باز می‌شود تا صفحه شلوغ نشود.
3. شماره ترتیب واقعاً جای فیلد را در فرم همان تب عوض می‌کند (۱ = اولین فیلد). دکمه‌های بالاتر/پایین‌تر هم همان کار را می‌کنند.
4. عنوان بخش کالا: «اضافه کردن کالا» (به‌جای کاتالوگ دارویی).
5. فیلد «دسته درمانی» حذف شد.
6. بعد از ثبت کالا، همان نام کالا در بنر سبز، داخل فیلد نام، و در ردیف جدول نمایان می‌ماند.

# نسخه ۱۱.۰.۰

1. تغییر رمز هر کاربر و اعمال در ورود بعدی (localStorage + state)
2. نشانگر میلادی امروز ایران
3. افزودن لحظه‌ای برای فیلدهای تایپی
4. شماره ردیف واقعی ۱،۲،۳ در لیست‌ها
5. خروجی اکسل با جدول خط‌کشی‌شده (.xls)
6. فعالیت لحظه‌ای: نمودار + نقشه
7. مختصات با دقت بیشتر و GPS دقیق
8. نقشه جامع ایران / منطقه / جستجو / مراکز شاخص
9. موقعیت زنده به تفکیک نماینده
10. تب شروع/پایان ویزیت با ادامه مسیر در نقطه کور
11. تارگت فروش با سال/ماه/کالا و جمع
12–13. طراحی فیلد برای همه تب‌ها + افزودن‌ها
14. تعداد جایزه کنار کالا در سفارش و لیست
15. مخفی‌سازی تب/فیلد بدون دسترسی
16. ریز عملیات در عیب‌یابی
