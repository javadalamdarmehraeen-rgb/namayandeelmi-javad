# تغییرات تکمیلی نسخه 8.1

این نسخه بر اساس فایل PDF بازسازی شده و اسکلت و تنظیمات قبلی حفظ شده است.

## تغییرات اصلی
- حذف کارت تکراری «دسترسی یکجا به ۲۰ قابلیت» از داشبورد.
- افزودن نمودارهای میله‌ای، دایره‌ای و خطی ساده و قابل توسعه در داشبورد.
- فعال‌سازی نمایش همزمان فرم ثبت و لیست در تب‌های داروخانه، پزشک و سفارش‌ها (Split View).
- تنظیم خودکار تاریخ جاری شمسی بر اساس منطقه زمانی Asia/Tehran.
- افزودن لایه بهبود `public/crm-enhancements.js` بدون بازنویسی هسته برنامه.
- اتصال دکمه موقعیت فعلی به Reverse Geocoding و پر کردن آدرس متنی.
- رندر پویا و ساده‌تر داده‌های ایران در `public/crm-data.js`.
- رفع اشکال‌های نحوی استخراج PDF و خطاهای رشته‌ای چندخطی.
- حفظ `server.js`, `render.yaml`, `package.json`, تنظیمات سلامت Render و PWA.

## اجرا
```bash
npm install
npm start
```
سپس روی `http://localhost:10000/login` باز شود.

## استقرار
همان تنظیمات قبلی Render/Neon باقی مانده است:
- Build Command: `npm install --include=dev && npm run build`
- Start Command: `npm run start`
- Health Check: `/api/health`
