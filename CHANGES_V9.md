# نسخه ۹.۱.۰ — طنین طب طاها

## چه چیزی واقعاً در این بسته تغییر کرده

اسکلت برنامه (هدر تیل، منوی بالا، سایدبار، فرم‌ها و نقشه‌های Leaflet فعلی) حفظ شده است. شکل نقشه ایران عوض نشده.

### انجام‌شده و در کد قابل مشاهده

1. لوگوی طنین طب طاها (کپسول‌های سبز + 3T) در هدر، سایدبار، صفحه ورود، Favicon و آیکون PWA.
2. صفحه ورود اولین صفحه است (گیت ورود). نمونه مدیر: `admin` / `123`.
3. فیلد «توضیحات لوکیشن / آدرس استخراج شده از نقشه» از فرم داروخانه و پزشک حذف شد.
4. دکمه «موقعیت فعلی من» هم مارکر را روی نقشه می‌گذارد و هم آدرس را در همان فیلد «لوکیشن و نقشه» می‌نویسد.
5. جستجوی آدرس، نقشه را روی همان نقطه زوم می‌کند.
6. تایپ در فیلد لوکیشن پیشنهاد لحظه‌ای آدرس می‌دهد (Autocomplete).
7. کلیک روی نقشه هم آدرس را در همان فیلد لوکیشن می‌نشاند.
8. کلیک روی «لیست» فرم ثبت را مخفی نمی‌کند؛ هر دو همزمان دیده می‌شوند.
9. دکمه‌های ثبت/ذخیره یک‌بار و با قفل ضدتکرار کار می‌کنند.
10. نشانگر تقویم، ماه و روز میلادی امروز را با منطقه زمانی ایران نشان می‌دهد.
11. لانچ‌پد پایین داشبورد حذف شد.
12. نقشه جامع: گزینه ایران، فیلد منطقه، دکمه جستجو، شمارش داروخانه/پزشک/بیمارستان، خروجی اکسل.
13. رصد تردد: Polyline مسیر + توقف‌ها + رنگ سبز/نارنجی.
14. منزل نمایندگان: فیلد آدرس + موقعیت فعلی من + نمایش روی نقشه.
15. فعالیت لحظه‌ای: نمودار + جدول.
16. موقعیت زنده: جستجوی نماینده روی نقشه.
17. ستون ردیف و نام نماینده، وضعیت لوکیشن، دکمه نقشه و مسیریابی، کلیک ردیف برای مودال.
18. سفارش: فیلد تعداد جایزه کنار هر کالا.
19. پیام‌رسان‌ها: تیک ارسال خودکار/دستی.
20. پشتیبان: تایم خودکار + ایمیل مقصد.
21. نصب اپ: لینک ویندوز / اندروید / آیفون + آیکون کپسول.
22. پروکسی `/api/geocode` و `/api/reverse` روی سرور برای کار بهتر جستجوی آدرس.

### انجام‌نشده (عمداً ادعا نمی‌شود)

- بازنویسی کامل React + Neon/PostgreSQL
- پیامک OTP واقعی اپراتور ایران
- هش رمز سمت سرور / 2FA واقعی / Obfuscation
- Push مستقیم به GitHub از این محیط (اعتبارنامه ندارم)

## استقرار روی سیستم خودتان

در پوشه پروژه:

```bat
git add .
git commit -m "v9.1.0 Tanin Teb Taha logo and two-way map location"
git push origin main
```

اگر Git هویت نخواست:

```bat
git config user.email "javadalamdar@example.com"
git config user.name "Javad Alamdar Mehraeen"
```

اگر push رد شد چون تاریخچه دور عوض شده:

```bat
git push origin main --force
```

Render بعد از push باید `npm start` را با `node server.js` اجرا کند.

## بررسی بعد از استقرار

- `https://namayandeelmi-javad.onrender.com/login` باید لوگوی کپسول را نشان دهد.
- در سورس صفحه باید `crm-features-v9.js?v=9.1.0` باشد.
- فیلد «توضیحات لوکیشن / آدرس استخراج شده» نباید دیده شود.
