# نسخه ۱۰.۰.۰

ورود جدا و سبک (`/login` حدود ۶ کیلوبایت). پنل بعد از ورود در `/panel`.
Leaflet محلی. فونت گوگل حذف شد. gzip روی JS/CSS/HTML.
فرم و لیست دیگر مخفی نمی‌شوند.
موقعیت فعلی من آدرس را همان لحظه در فیلد لوکیشن می‌نویسد.
ویجت‌های داشبورد قابل افزودن/حذف توسط مدیر.
۴۲ مرکز شاخص درمان در ۳۱ استان برای نقشه جامع و جستجو.
عیب‌یابی تصویری.
ورود: admin / 123
